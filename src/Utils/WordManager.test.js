import WordManager from './WordManager';

describe('Test word manager scenarios', () => {
   let manager = new WordManager("testFile");
   it('should get word with specified length', () => {
      // expect(manager.getWord(4)).expect(x => x.length <= 4);
      // expect(manager.getWord(5)).expect(x => x.length >= 5 && x.length <= 8);
   });
});


